<%= form_for(@usach_complaint) do |f| %>
  <% if @usach_complaint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usach_complaint.errors.count, "error") %> prohibited this usach_complaint from being saved:</h2>

      <ul>
      <% @usach_complaint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-field">
    <%= f.label :contenido %>
    <br>
    <br>
    <%= f.text_field :content, required: true, id: 'x', class: 'hidden' %>
    <trix-editor input="x"></trix-editor>
  </div>
  <div class="actions">
    <%= f.submit "Guardar" %>
  </div>
<% end %>
